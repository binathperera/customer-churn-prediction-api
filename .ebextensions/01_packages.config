packages:
  yum:
    libgomp: []
    
commands:
  01_setup_swap:
    command: |
      if [ ! -f /swapfile ]; then
        fallocate -l 2G /swapfile
        chmod 600 /swapfile
        mkswap /swapfile
        swapon /swapfile
      fi
    ignoreErrors: true